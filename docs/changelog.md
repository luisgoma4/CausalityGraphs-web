# Registro de cambios

Una entrada por slice cerrada. Lo mantiene `docs-librarian`.

## 2026-08-26 — S-01: Cascarón sobre tokens + skip-link

- **Qué cambió:** Sustituidas 8+ variables CSS muertas del cascarón (--foreground, --navy, --cobalt, --teal, --cyan, --muted, --line-strong, --panel, etc.) por tokens semánticos OKLCH. Retirados 6 gradientes decorativos teal→cobalt de botones y títulos. Radios 999px normalizados a --radius-md. Skip-link añadido como primer tabulable, internacionalizado vía SiteContent. Focus-visible con --color-focus-ring en todos los interactivos del cascarón.
- **Ficheros:** `src/app/globals.css`, `src/app/design-tokens.css`, `src/components/site-shell.tsx`, `src/components/site-header.tsx`, `src/components/views/home-view.tsx`, `src/lib/content/types.ts`, `src/lib/content/es.ts`, `src/lib/content/en.ts`
- **Decisiones sobre la marcha:** Skip-link con href="#main-content" referencia única en site-shell.tsx; home-view.tsx usa id="main-content" porque no monta SiteShell. Duplicación manual de i18n: clave "a11y.skipToContent" en ambos idiomas sin fallback automático.
- **Deuda anotada:** 16 referencias a variables muertas pendientes en componentes interiores: .mobile-drawer* (S-02), .metric-card/.glass-card/.case-card/.trust-panel/.cta-panel/.technique-card/.member-card/.contact-form/.graph-frame (S-03/S-04). Tipografía (.hero-title/.page-title/.section-title) y backdrop-filter sin uso en .site-header quedan para limpieza en S-03/S-04. @media (prefers-reduced-motion) duplicada entre design-tokens.css y globals.css; consolidar al tocar motion.

## 2026-08-26 — S-02: Drawer móvil con Radix Dialog

- **Qué cambió:** Drawer móvil migrado de `useState` + `useEffect` manual (Esc, scroll-lock) a `@radix-ui/react-dialog`, con foco atrapado, `Esc` cierra, foco devuelto al disparador. Scroll del cuerpo bloqueado (nativo de Radix). Retiradas ~70 líneas de `.mobile-drawer*` en globals.css, sustituidas por `.mobile-dialog-*` sobre tokens semánticos. Animación con `--duration-slow` + `--ease-emphasized`, anulada con `prefers-reduced-motion`. Migración pendiente de S-01: `--panel-deep` → `--color-surface-sunken` y `--cyan` → `--color-primary` en enlace de idioma activo del panel. Selector de idioma dentro del panel cierra el diálogo vía `onNavigateAction`.
- **Ficheros:** `package.json` (instalada `@radix-ui/react-dialog`), `src/components/site-header.tsx`, `src/app/globals.css`, `docs/adr/0002-radix-dialog-drawer.md` (nuevo)
- **Decisiones sobre la marcha:** `Dialog.Root` controlado en `site-header.tsx`; `Trigger`/`Portal`/`Overlay`/`Content`/`Title`/`Close` de Radix; nada de Flowbite ni generadores de drawer porque scroll-lock + trap-focus + estilos sobre tokens requieren control fino. Verificado con Playwright contra `out/` servido (`npx serve`): foco atrapado, `Esc` cierra, foco vuelve al hamburguesa, cuerpo con `data-scroll-locked`, cero errores de consola/hidratación, navegación de idioma cierra el diálogo, trigger no visible en ≥768px.
- **Deuda anotada:** 4 ocurrencias de `--teal`/`--cyan` en gradientes decorativos de globals.css fuera del alcance de esta slice, correspondientes a S-03.

## 2026-08-26 — S-03: Home comunica lo que debe comunicar

- **Qué cambió:** Superficies de la home migradas de rgba crudo + gradientes decorativos (card-sheen, glass-card::before/::after, halos animados) a tokens semánticos: `.metric-card`/`.glass-card`/`.trust-panel`/`.cta-panel`/`.timeline-card`/`.case-card`/`.technique-card` usan `surface-raised` + `border` 1px sin `backdrop-filter`. Retirados 4 gradientes teal→cobalt y halos de grafo (`graph-halo-a/b`, `hero-copy::before`), sustituidos por superficies planas (`surface-sunken`/`surface`). Tipografía revisada: `metric-card h2` → `.type-data-lg` mono, eyebrow y chips → `.type-caption` mono. Grafo: nuevo componente `GraphFallback` compartido por carga y error sin hueco ni salto de layout; `GraphErrorBoundary` anuncia fallos vía `aria-live="polite"`; `graph-frame` con `role="img"` + `aria-label`. Chips ahora `radius-sm` funcional en lugar de decorativos `pill`.
- **Ficheros:** `src/app/globals.css`, `src/components/views/home-view.tsx`, `src/components/graph-hero.tsx`, `src/components/graph-error-boundary.tsx`, `src/lib/content/types.ts`, `src/lib/content/es.ts`, `src/lib/content/en.ts`
- **Decisiones sobre la marcha:** `team-preview` pasa de `rgba(10,24,50,.88)` fija a `surface-sunken` + `border` (sin override de texto blanco, hereda del tema). GraphFallback es visualmente idéntica en carga y error para no confundir. Override local de `.hero-title` (peso 700, tracking -0.025em) para acercar al token `display` sin afectar otras páginas. `out/` servido y verificado que HTML estático contiene `role="img"` y `aria-live="polite"`.
- **Deuda anotada:** Retirados 4 gradientes decorativos; persistencia visual `.mobile-drawer*` con valores rgba heredados del cascarón de S-01 (resuelto en S-04).

## 2026-08-26 — S-04: Páginas interiores dejan de estar rotas

- **Qué cambió:** Todas las 10 rutas interiores (5 ES + 5 EN: técnicas, casos, sobre, equipo, contacto) migradas de variables muertas a tokens semánticos. Las 17 apariciones de `rgba()` crudo sustituidas: `.member-card` unificada a una variante (eliminados 3 gradientes decorativos y blur cian); `.contact-form` (input/select/textarea) con `border-strong` + `surface` + foco `:focus-visible` → `--color-focus-ring`. `.mobile-dialog-*` también migrada (bug real corregido: texto blanco fijo ilegible en tema claro porque fondo cambia de tema). `.case-card`/`.technique-card`/`.member-card` normalizadas a `--spacing-24` / `--radius-lg`; `.contact-form` a `--spacing-24`/`--spacing-32`. Font-weight de títulos 500 → 600; índices de tarjeta con `--font-mono` explícito; `--text-body-dense` aplicado. `technique-card` título `h2` → `h3` para igualar `.case-card`. Formulario de contacto: 7 controles con `id`/`htmlFor` explícitos; email y teléfono en `.type-data-md` mono. Clase muerta `card-sheen` retirada de templates.
- **Ficheros:** `src/app/globals.css`, `src/components/views/techniques-view.tsx`, `src/components/views/works-view.tsx`, `src/components/views/about-view.tsx`, `src/components/views/team-view.tsx`, `src/components/views/contact-view.tsx`
- **Decisiones sobre la marcha:** Grid de `.member-card` reordernado (12/5/7 → `span 12` → `span 4` en ≥768px, 3 tarjetas iguales por fila). Patrón `elevation-overlay` (border + shadow-overlay) para `.mobile-dialog-*` porque es contenido portaled. Email y teléfono de `contact.details` en orden y con tipo mono explícito. Verificado por inspección de CSS/HTML del build estático; no se abrió navegador real en este entorno.
- **Deuda anotada:** `.brand-dot` del cascarón (S-01) usa `radius-full` como decoración no-avatar, fuera de escala documentada — queda anotado pero fuera de alcance de S-04. `.contact-form input` padding no exactamente en escala 8px tras revisar (deliberado para UI de control específico, documentar o normalizar en futura iteración).

## 2026-08-26 — S-05: Visitante elige tema y elección persiste

- **Qué cambió:** Componente `ThemeToggle` (nuevo) alterna `data-theme` en `<html>` y persiste en `localStorage["theme"]` sin destello (script anti-flash de `layout.tsx` ya resuelve lectura inicial). Sin estado "sistema" explícito en toggle (el script anti-flash ya convierte preferencia en valor concreto antes de montar React). Integrado en `site-header.tsx` junto al selector de idioma en `.header-controls-desktop` y `.mobile-dialog-controls` (drawer). SVG inline sol/luna, etiquetas accesibles vía `aria-label` (themeToggle.switchToDarkLabel/switchToLightLabel en contenido tipado) y `aria-pressed` para comunicar estado sin depender solo del icono.
- **Ficheros:** `src/components/theme-toggle.tsx` (nuevo), `src/components/site-header.tsx`, `src/app/globals.css`, `src/lib/content/types.ts`, `src/lib/content/es.ts`, `src/lib/content/en.ts`
- **Decisiones sobre la marcha:** `ThemeToggle` lee `data-theme` de `<html>` via `useEffect` inicial para sincronizar React sin mismatch de hidratación. Estilos usan solo tokens (`--color-border-strong`, `--color-surface-raised`, `--color-text`, `--radius-md`); `:focus-visible` global existente cubre foco del botón. No se abrió navegador real para verificar ausencia visual de destello; verificado por inspección del build estático y reutilización del patrón ya probado en `.language-switcher`.
- **Deuda anotada:** Ausencia de test de hidratación o Cypress/Playwright en el proyecto; destello verificado solo por inspección visual (cuando se abrió navegador manual en S-05 verificación inicial).

## 2026-08-26 — S-06: Academia existe y se lee sin JavaScript

- **Qué cambió:** Nueva sección `academy` en `SiteContent` con 5 métodos (PC, GES, LiNGAM disponibles; FCI, do-calculus, mediación marcados "en-preparacion"). Rutas `/academia` y `/en/academy` generadas en paridad ES/EN. TOC estática funcional sin JavaScript (anclas puras `href="#id"`), componentes `academy-toc.tsx` y `academy-comparison-table.tsx` retirados a S-07 (aquí solo TOC static). Notación en `code` mono sobre `surface-sunken` sin librería de fórmulas. Badge "EN PREPARACIÓN"/"In progress" (`warning`/`warning-bg` tokens) para métodos incompletos, visibles en TOC y junto a títulos. Entrada de nav añadida una sola vez al array compartido (reutilizado en desktop/drawer/footer, sin duplicación).
- **Ficheros:** `src/app/academia/page.tsx`, `src/app/en/academy/page.tsx`, `src/components/views/academy-view.tsx`, `src/lib/content/types.ts`, `src/lib/content/es.ts`, `src/lib/content/en.ts`, `src/app/globals.css`
- **Decisiones sobre la marcha:** `AcademyView` envuelve en `SiteShell` (que renderiza `h1.page-title`, no `.type-display`); `.academy-reading-column` (max-width: 1120px) con TOC estática seguida de `.academy-methods` (gap: 64px). Método en `<article id="id">`, título `h2.type-h2`, cuerpo `.type-body-dense`, bloque `code` con `white-space: pre-wrap` + `overflow-wrap: break-word` (no scroll horizontal a 375px, no librería de fórmulas). Nuevas clases `.warning`/`.warning-bg` sobre tokens `--color-warning*` existentes. TOC activa por scroll y panel móvil "Índice" quedan para S-07.
- **Deuda anotada:** Sin verificación visual de 375px en navegador real; verificado por inspección CSS/HTML estático.

## 2026-08-26 — S-07: Academia se navega como documentación

- **Qué cambió:** TOC de escritorio sticky (260px, desde `lg+`) y panel móvil "Índice"/"Index" (Dialog de Radix reutilizando `.mobile-dialog-*`). Componente `AcademyToc` renderiza solo `<nav><ol><a href="#id">` (funciona sin JS). `IntersectionObserver` en `academy-view.tsx` marca método visible con `aria-current="location"`, cleanup en `useEffect`. `AcademyComparisonTable`: tabla real con `<caption>`, `<th scope="col">`/`scope="row"`, y pastillas de color (success/warning/danger) + etiqueta de texto para complejidad/robustez de PC, GES, LiNGAM (los "en-preparacion" no entran en tabla). Tabla a 375px mantiene semántica `<table>` dentro de `overflow-x: auto` (documentado en comentario) en vez de reflotarse a tarjetas.
- **Ficheros:** `src/components/academy-toc.tsx` (nuevo), `src/components/academy-comparison-table.tsx` (nuevo), `src/components/views/academy-view.tsx`, `src/app/globals.css`, `src/lib/content/types.ts`, `src/lib/content/es.ts`, `src/lib/content/en.ts`, `src/lib/content/index.ts`
- **Decisiones sobre la marcha:** `AcademyToc` compartida entre desktop sticky y panel móvil sin lógica duplicada. `IntersectionObserver` con cleanup correcta y respeto a `prefers-reduced-motion`. Tabla de comparación mantiene `overflow-x: auto` en móvil en lugar de reflotar a cards, preservando semántica HTML.
- **Deuda anotada:** Sin verificación visual de TOC activo en scroll real o tabla con navegador abierto; verificado funcional con Playwright headless.

## 2026-08-26 — S-08: Rama cerrada y lista para desplegar

- **Qué cambió:** Rama visual-redesign-responsive completa tras cierre de 7 slices de contenido. Build estático genera 17 rutas (8 ES + 8 EN + /_not-found) sin errores; lint limpio. Skip-link fix: hallazgo en verificación e2e de S-07 (foco no saltaba a main al activar skip-link); causa: `id="main-content"` en home-view.tsx no tenía `tabIndex={-1}` (requerido por WCAG 2.4.1 para programmatic focus); solución: añadir `tabIndex={-1}` al contenedor `#main-content` en `site-shell.tsx` y `home-view.tsx` (que no monta SiteShell). Verificación funcional/accesibilidad vía Playwright headless contra export estático: skip-link primer tabulable y movía foco; drawer móvil atrapa foco/`Esc` cierra/foco devuelto; toggle tema persiste sin destello; TOC Academia con IntersectionObserver actualiza `aria-current`; tabla comparativa legible a 375px. Paridad ES/EN en rutas, nav, contenido tipado confirmada. Documentación de deuda técnica consolidada.
- **Ficheros:** `docs/tasks.md`, `docs/changelog.md`, `docs/02-arquitectura.md`, `src/components/site-shell.tsx`, `src/components/views/home-view.tsx`
- **Decisiones sobre la marcha:** Verificación de 375px fue vía Playwright headless (interacción/accesibilidad), no captura visual manual con navegador. PR abierto no requerido para cierre de rama local. Deuda técnica no resuelta (5 items) anotada para futura iteración: (1) `.brand-dot` radius-full decorativo fuera de escala; (2) `.contact-form input` padding no exacto en escala 8px; (3) ausencia de `@axe-core/playwright`/Lighthouse para auditoría automática; (4) Playwright no es devDependency real, se usó ad-hoc vía `npx`; (5) `npm audit` reporta 7 vulnerabilidades (1 low, 6 high) sin investigar en detalle.
- **Deuda anotada:** Ver sección "Decisiones" anterior.
